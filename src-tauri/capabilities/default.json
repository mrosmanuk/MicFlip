{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for MicFlip windows (settings, HUD).",
  "windows": ["*"],
  "permissions": [
    "core:default"
  ]
}
